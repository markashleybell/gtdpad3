@model IndexViewModel

<div class="col">

    <h1>@(Model.Page.Title)</h1>

</div>
<div class="col">

    <ul>

        @foreach (var page in Model.Pages)
        {
            <li>@(page.Title)</li>
        }
    </ul>
</div>

@section scripts {

}
